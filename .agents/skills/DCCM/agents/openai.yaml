interface:
  display_name: "DCCM"
  short_description: "Repo-specific patterns and workflows for DCCM"
  default_prompt: "Use the DCCM repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true